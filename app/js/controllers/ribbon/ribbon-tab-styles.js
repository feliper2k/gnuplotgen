'use strict';

let tabs = {
    'Current dataset': [{
        type: 'custom',
        name: 'stylesCurrent'
    }],

    'Line style': [{
        type: 'custom',
        name: 'stylesAppearance'
    }],

    'Grid style': [{
        type: 'custom',
        name: 'stylesGrid'
    }]
};

export default tabs;
