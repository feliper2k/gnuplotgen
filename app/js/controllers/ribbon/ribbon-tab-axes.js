'use strict';

let tabs = {
    'Active axis': [{
        type: 'custom',
        name: 'axesChooser'
    }],

    'General': [{
        type: 'custom',
        name: 'axesGeneral'
    }],

    'Tics style': [{
        type: 'custom',
        name: 'axesMarkers'
    }],

    'Tics settings': [{
        type: 'custom',
        name: 'axesMarkersAdvanced'
    }]
};

export default tabs;
