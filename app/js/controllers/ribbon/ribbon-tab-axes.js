'use strict';

let tabs = {
    'Active axis': [{
        type: 'custom',
        name: 'axesChooser'
    }],

    'General': [{
        type: 'custom',
        name: 'axesGeneral'
    }],

    'Markers': [{
        type: 'custom',
        name: 'axesMarkers'
    }]
};

export default tabs;
