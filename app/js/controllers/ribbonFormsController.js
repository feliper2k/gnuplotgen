'use strict';

function gpRibbonFormsController(ribbonModel) {
    'ngInject';

    const vm = this;
    // vm.rbm = ribbonModel;

    vm.rbm = {
        title: "23r2323"
    };
}

export default {
    name: 'gpRibbonFormsController',
    fn: gpRibbonFormsController
};
